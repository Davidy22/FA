name: Keepalive
on:
  schedule:
    - cron: '0 14 * * 3'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsSL ${{ vars.STAGING_HEALTH_URL || 'https://example.com' }} || echo "Ping failed (free tier may have paused)."
